import spacy

nlp = spacy.load("en_core_web_sm")
doc = nlp("This is the first sentence. This is the second, cooler sentence, which is more complicated than the first")
for sent in doc.sents:
    print(sent.head)
